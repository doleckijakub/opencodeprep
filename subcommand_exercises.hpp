#pragma once

#include "os.hpp"

namespace ocp
{
    namespace exercises
    {
        void pull();

        // struct ListOptions
        // {
        // 	int results_per_page = 20;
        // 	int page = 1;
        // };

        void list(/* ListOptions &options*/);
    } // namespace exercises

} // namespace ocp
